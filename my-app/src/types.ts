export type PostType = {
    id: number;
    title: string;
    content: string;  // content?: string; optional with ?
}

export type User = {
    id: number;
    name: string;
    email: string;
}

export type UserType = {
 id: number;
 name: string;
 email: string;
}